import { blue } from '@material-ui/core/colors'

export default (theme) => ({
  top: {
    display: 'flex',
    justifyContent: 'space-between',
    minWidth: '100px',
  },
  icons: {
    fontSize: 'small'
  },
  buyButton: {
    color: blue[600]
  }
})
