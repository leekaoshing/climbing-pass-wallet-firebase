export default (theme) => ({
  avatarCurrent: {
    width: '100%',
    maxWidth: '13rem',
    marginTop: '1.5rem',
    height: 'auto',
    cursor: 'pointer'
  },
  gridItem: {
    textAlign: 'center',
    marginTop: theme.spacing(0)
  }
})
