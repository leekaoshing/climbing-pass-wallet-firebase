rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gyms/{document=**} {
      allow read;
    }

    match /users/{user} {
      allow read;      
      allow write: if request.auth.uid == user;
    }
  }
}
